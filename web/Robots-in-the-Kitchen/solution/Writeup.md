## Robots-in-the-Kitchen

This challenge is a very simple path traversal I think, but with a twist, you would never find out the name of the directory you need to go in if you not read the robots.txt

The Challenge is a bit guessy I think but I also think it makes sense.

* Look at the /robots.txt
* There you will find secret directory and flag.txt file

`/secret-admin-backup/flag.txt`

That is basically everything.