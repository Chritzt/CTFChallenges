## Broken Prison

The Challenge is a python Jail, it blocks most things like import, __ or FLAG.

The intended solution is to run breakpoint(), then go in the stack up with the u command in breakpoint and then print(FLAG)